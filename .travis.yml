language: node_js
node_js:
  - "v10.15.2"

cache:
  yarn: true
  directories:
    - node_modules

script:
  - yarn lint
  - yarn test
  - yarn build

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: build
  on:
    branch: master


