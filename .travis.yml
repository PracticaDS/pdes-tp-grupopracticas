language: node_js
node_js:
  - "v10.15.2"

cache:
  yarn: true
  directories:
    - node_modules

script:
  - cd frontend && yarn lint && yarn test && yarn build && cd ..

deploy:
  provider: pages
  skip_cleanup: true
  github_token: $github_token
  local_dir: frontend/build
  on:
    branch: master


